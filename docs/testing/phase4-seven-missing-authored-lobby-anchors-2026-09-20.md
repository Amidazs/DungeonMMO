# Seven missing authored TEST Lobby anchors — 20 September 2026

Status: **PREVIEW-FIRST REPAIR PREPARED, NO CLOUD CHANGES**. The user's original cloud Studio Edit-mode inspection found these required Base anchors missing: `Base.Trainer.Rogue`, `Base.Service.Bank`, `Base.Service.Travel`, `Base.Travel.Market`, `Base.Travel.Dwarves`, `Base.Travel.Portals`, and `Base.Travel.Orcs`. The first guarded one-anchor repair correctly refused to modify the place. `BaseRuntime` failed to start due to the missing Rogue anchor, explaining stuck race confirmation. `SkillsMenu` also lacked `ReplicatedStorage.Combat.Shared`; corrected `test-lobby-sync.project.json` already passes its independent composition QA.

### Grounded world geometry

The read-only cloud audit confirmed existing `Base.TrainingArea`, `Base.ArrivalFromTemple` (a required anchor, not printed by the filtered audit), `Base.TemplePortal`, `Base.Service.BankPlaceholder` at approximately (-20,17.2,16.2), and `Base.Service.TravelPlaceholder` at approximately (20,17.2,0.2). It also showed `Market_SocialFurniture` with meshes at x≈0,z≈-19, the actual `Portal_02` geometry elevated at x≈0,z≈96, and existing `FastTravel_Dwarf_Surface_Mine_dais` and `FastTravel_Orc_Farm_dais` at approximately (124,41,81) and (-147,16,48). These are authored world positions; parent-model pivots in this imported scene can be inaccurate. The audit stopped after 65 candidate objects; there is no basis to claim its printed list includes every travel object.

### Source / application boundary

`scripts/studio/repair_test_cloud_lobby_seven_anchors.commandbar.luau` on isolated integration branch `wip/phase-4-test-hud-integration-v1` starts with `APPLY=false`. Run via Studio Command Bar **Edit mode only** in existing cloud Lobby place 134132328219009 / universe 10765241947. It refuses other places, duplicate required IDs, or missing required IDs outside the seven, and calculates all seven proposals before any mutation. The new canonical Bank/Travel anchors use the existing labelled placeholders' exact CFrames without deleting or retagging them. The Rogue trainer goes beside the already-authored TrainingArea. The Market travel/auction marker uses the existing ArrivalFromTemple anchor only if it is near the genuine market furniture bounds. Dwarf and Orc destinations use real fast-travel dais geometry bounds rather than broken parent pivots. The Portal destination uses the existing candidate profile's Portal_02 geometry-bound fraction and +10-stud offset, subject to a proximity check against the existing Temple portal. The preview prints all proposed positions, changes nothing and requires the owner to review the proposals. For **APPLY=true**, the owner MUST first back up the CURRENT AUTHORED CLOUD LOBBY via File > Save to File and note current Roblox Version History. The Apply path creates only missing canonical parts, validates all required anchors, rolls back the newly created parts if validation fails, and is idempotent. The generated parts are invisible gameplay markers, not new NPCs, buildings or portal meshes. It never touches player profiles, cloud DataStores, code release gates, PROD places or teleport destination IDs.

Local **unpublished, disposable Studio edit-mode** fixture `20260920T090316Z_Studio_C40AA_last.log`: seeded exact seven missing anchors and synthetic geometry stand-ins with named landmarks; preview yielded zero changes; Apply created seven, full resolver contract passed, repeat Apply created zero duplicates; zero Creator errors; `PREVIEW_APPLY_IDEMPOTENCE_PASS`. This verifies code behaviour, NOT that the proposed locations are walkable in the actual authored map. All seven proposed real cloud positions must be reviewed in the cloud preview, tested in Studio Play, and the player must physically test each fast-travel destination for walkability before a cloud publish. The updated lobby sync additionally includes `Combat.Shared.SkillDefinitions`, but must be used as a **sync-only project** on the existing cloud Lobby, never published as a full replacement over its authored village.

**No cloud backup was verified, no repair was applied to the user's existing cloud place, and no Roblox cloud place was published in this iteration.**
