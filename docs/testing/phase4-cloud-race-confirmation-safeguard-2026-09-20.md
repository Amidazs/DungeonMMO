# Phase 4 cloud race confirmation safeguard — 20 September 2026

Status: **tested local safeguard, not yet published to cloud**. User reports that the published/cloud Lobby shows a selectable Human/Elf card but clicking Confirm does nothing. The supplied screenshot still reads "Elf selected. Press Confirm to continue." The separate local Studio log confirms a normal Elf > Mage flow. These observations do not establish whether the cloud Confirm button receives an Activated event or whether an earlier request remains stuck. Do not attribute the cloud symptom to profile data or claim it is resolved without authenticated cloud evidence.

The original integrated HUD and backend branches and the existing Roblox cloud places remain untouched. No player profile or DataStore was reset or edited in this work.

Two narrow source safeguards on isolated integration branch `wip/phase-4-test-hud-integration-v1`:

- `IdentitySelection.client.luau`: log that a manual Race/Class request fired; show "Saving ... choice..." immediately; if no authoritative stage/result arrives within 12 seconds, restore the Confirm button and show "No response from server. Press Confirm to retry." Request a fresh progression snapshot. Never set RaceId/ClassId client-side, and never clear the player's authoritative profile.
- `BaseRuntime.server.luau`: if `loaded_users` is false on a Race/Class request, return a `ProgressionActionResult` with `IdentitySelection`, `ok=false`, `reason="ProfileNotLoaded"` instead of silently discarding it. No profile mutation in this failure path.

Focused TEMP test `scripts/studio/phase4_base_race_confirm_missing_response_live_probe.luau` deliberately suppresses the client-to-server race request in a disposable, unpublished Base DataModel. The initial fixture failed to suppress the request because its string substitution did not escape the literal parentheses; this was corrected. The retest, log `20260920T082226Z_Studio_13518_last.log`, passed: manual request UI, 12-second timeout/retry, no profile write, 0 Creator errors. Marker: `[Phase4 Race Timeout] VERIFIED_PLAY_MODE_PASS`. This proves recovery after an unanswered request; it does **not** simulate an actual physical click or diagnose an unseen cloud button obstruction.

Post-change local tests: four Rojo compositions built (Base, Dungeon, TEST Temple, TEST Lobby sync); 559 source files + 45 Studio runners compiled, 0 errors. Fresh Base Play 107 pass markers/0 Creator errors (`20260920T082331Z_Studio_E02EB_last.log`), restored Base HUD 107/0 (`20260920T082401Z_Studio_B43C0_last.log`), Dungeon 221/0 (`20260920T082423Z_Studio_61D92_last.log`). Tested implementation source commit before this documentation: `c755dc4de04c2c956841ba1d22e65f4c457ec030`.

**Cloud next step:** In the actual published Lobby place 134132328219009, select race then press Confirm ONCE and observe lower-left text. If it remains "... selected. Press Confirm..." then the client button handler did not run or was inactive; inspect GUI focus/occlusion and actual cloud LocalScript version. If it changes to "Saving race choice..." then the client handler fired; inspect live cloud server/runtime logs and `ProgressionActionResult` / profile-load state. Collect Creator/Developer Console client and (if available) server errors from the same session before inferring cause. Test current published place, not synthetic local Base. Do not delete any more profiles. Sync any approved change into the EXISTING authored cloud Lobby via `test-lobby-sync.project.json`, after backing up the actual cloud place; do not overwrite its buildings/teleport attributes with a synthetic build. Do not claim cloud publication without Roblox Version History evidence.
