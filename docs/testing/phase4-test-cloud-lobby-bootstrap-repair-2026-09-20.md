# TEST cloud lobby bootstrap repair — 20 September 2026

**Status:** exact logged root cause identified; repair script and sync composition verified locally; **NOT applied to or published in the authenticated cloud Lobby**.

Target existing cloud Lobby: place `134132328219009`, universe `10765241947`. Existing authored village, portal settings and user profile/DataStores must remain untouched.

The user-provided cloud Studio output shows `BaseEnvironmentBootstrap: MissingAnchor:Base.Trainer.Rogue` followed by `BaseRuntime: Base environment invalid`. `BaseRuntime` terminates before connecting the server-side `IdentitySelectionRequest.OnServerEvent` handler. A client can still highlight a race, but the authoritative server never processes Confirm; clearing profile data cannot repair an absent map anchor or stopped runtime. An independent `SkillsMenu` infinite yield on `ReplicatedStorage.Combat` shows the old TEST Lobby Rojo sync composition was missing `Combat.Shared`. That does not explain the initial BaseRuntime crash, but is another genuine missing dependency.

Corrective files, isolated integration branch only:

- `scripts/studio/repair_test_cloud_lobby_rogue_anchor.commandbar.luau`: **Studio EDIT-mode only**, checks exact TEST universe/place, prints all missing required Base anchor IDs, refuses duplicates or missing unrelated anchors, creates `Base.Trainer.Rogue` through the existing `EnvironmentAnchorFactory` beside a unique authored Rogue NPC or, if unavailable, beside the existing training-area anchor. The fallback is a temporary invisible marker, to be repositioned later. It verifies all required anchor contracts and reports whether Combat skill definitions are still missing. It is idempotent and does **not** alter profiles or DataStores. **Before running on the actual cloud place, the owner must save a rollback copy of the authored cloud place.**
- `test-lobby-sync.project.json`: supplies `ReplicatedStorage.Combat.Shared` for the restored `SkillsMenu`, with `$ignoreUnknownInstances=true` under Combat to protect unrelated authored objects. This is a Rojo **sync-only** project and must NEVER replace the entire cloud lobby or authored map.
- `scripts/studio/phase4_restored_hud_publish_composition.luau`: verifies restored HUD code plus `Combat.Shared.SkillDefinitions` exists in the exact lobby sync composition while Dungeon-only client combat scripts remain excluded.

Local code/fixture evidence:

1. `repair_test_cloud_lobby_rogue_anchor.commandbar.luau` compiled. In an isolated unpublished **TEMP Base** DataModel, seeding every required Base anchor except Rogue, a disposable test-only version of the exact Command Bar script successfully created Rogue, verified the full contract and Combat.SkillDefinitions, then on second execution reported no missing anchors and created no duplicate. The assertions and logged behaviour passed. Two unrelated Roblox Ribbon plugin shutdown Creator errors followed during Studio exit, so do not describe this run as zero Creator errors. Log `20260920T085025Z_Studio_64D5C_last.log`. Neither authenticated cloud place nor any DataStore was touched.
2. The updated exact `test-lobby-sync.project.json` Rojo build passed. An unpublished Studio composition run logged `LOBBY_COMBAT_SKILL_DEFINITIONS_PASS`, `LOBBY_HUD_SYNC_ONLY_PASS`, `RESTORED_HUD_SOURCE_PASS`, with **zero Creator errors**. Log `20260920T085123Z_Studio_93104_last.log`.
3. Previously tested local Base/HUD and Dungeon gameplay regressions, plus a race-confirmation no-server-reply safeguard, remain recorded in `docs/testing/phase4-cloud-race-confirmation-safeguard-2026-09-20.md`. Those safeguards alone cannot restart a server runtime that has already failed an environment contract.

Cloud handoff requires human authenticated Studio edit-mode access. Save File > Save to File from the **existing cloud Lobby** and note Roblox Version History before changing it. Copy the commandbar Lua file into the Studio Command Bar in that cloud Lobby while Play is stopped, then run; wait for `[TEST Lobby Repair] ALL_REQUIRED_BASE_ANCHORS_VALID`. If it refuses due to multiple/missing unrelated anchors, stop and inspect the emitted IDs rather than turning off validation. Next Rojo sync `test-lobby-sync.project.json` into the SAME authored cloud Lobby, preserve its Workspace and TEST teleport attributes, and inspect Studio Play for `[Base Environment] Ready in Authored mode.`, successful Race > Class selection and no SkillsMenu missing Combat warning. Only then publish the **existing place** (not the synthetic Base build); verify race/class selection in an actual published cloud session. Record place version ID and tested outcomes after actual publication.

**No claim of cloud repair, publish, rollback backup or profile reset is justified by the local evidence alone.**
